package lk.ijse.posspringbackend.util;

public class RegexUtil {

    // Regex pattern for Customer ID
    public static final String CUSTOMER_ID_REGEX = "^[a-zA-Z0-9]+$";

    // Regex pattern for Item ID
    public static final String ITEM_ID_REGEX = "^[a-zA-Z0-9]+$";

    // Regex pattern for Order ID
    public static final String ORDER_ID_REGEX = "^[a-zA-Z0-9]+$";

    // Regex pattern for Order Detail ID
    public static final String ORDER_DETAIL_ID_REGEX = "^[a-zA-Z0-9]+$";

}
